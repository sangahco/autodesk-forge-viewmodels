version: "3"

services:

  bimviewer:
    build: .
    logging: 
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    environment: 
    - FORGE_CLIENT_ID
    - FORGE_CLIENT_SECRET
    ports:
    - ${HTTP_PORT}:3000